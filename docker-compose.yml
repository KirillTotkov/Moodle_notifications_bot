version: "3.9"

services:
  postgres:
    image: postgres
    restart: always
    env_file:
      - .env
    ports:
      - "5432:5432"

  bot:
    build: .
    restart: always
    env_file:
      - .env
    deploy:
      resources:
        limits:
          cpus: '0.4'
    depends_on:
      - postgres
